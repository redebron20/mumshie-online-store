<h1> Categories </h1>

<div class="new-form">
        <% @categories.each do |category| %>
            <p><%= link_to category.name, category %>(<%= category.products.size %>)</p>
            <p><%= category.description %></p>
        <% end %>
    </ul>
</div>