<!DOCTYPE html>
<html>
  <head>
    <title>MumshieOnlineStore</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <nav>
    <div class="navbar-list">
    
     <% if logged_in? %>
        <a class="navbar-link <%= active_css(root_path)%>" href="/">Home</a>
        <a class="navbar-link <%= active_css(categories_path)%>" href="/categories">Categories</a>
        <a class="navbar-link <%= active_css(products_path)%>" href="/products">Products</a>
        <a class="navbar-link <%= active_css(new_category_path)%>" href="/categories/new">New Category</a>
        <a class="navbar-link <%= active_css(new_product_path)%>" href="/products/new">New Product</a>
        <a class="navbar-link <%= %>" href="/carts/show">View Cart</a>
        <%= button_to "Logout", logout_path, method: :delete, class: "navbar-link active_css(root_path)" %>
      <% else %>
        <%= button_to "Log In", login_path, method: :get, class: "navbar-link active_css(root_path)" %>
        <%= button_to "Sign Up", signup_path, method: :get, class: "navbar-link active_css(root_path)" %>
      <% end %>
      
      </div>
    
  </nav>
  <div class="content">

    <div class="content" style="background-image: url('/img/dot-grid.png')">

      <% if flash[:success] %>
        <p class="success"><%= flash[:success] %></p>
        <%else flash[:error]%>
        <p class="error"><%= flash[:error] %></p>
      <% end %>
    

      <%= yield %>
    </div>
  </div>
</body>
<footer class="site-footer">

  <div class="container">
    <div class="row">
      <p class="copyright-text">Copyright &copy; 2021 All Rights Reserved by Mumshie.</p>
    </div>
  </div>
</footer>
</html>
