<h2>Order show</h2>
<ul>
  <% @order.order_items.each do |order_item| %>
    <li>Item: <%= order_item.product.name %>/ Price: <%= number_to_currency(order_item.product.price) %>/ Quantity: <%= order_item.quantity %>/ Total Price: <%= number_to_currency(order_item.total_price) %>
    # See orderItem section below for explanation of order_item links
    <ul>
      <li>
        <%= link_to "Add one", order_item_add_path(:id => order_item), method: :post %>
        <%= link_to "Reduce one", order_item_reduce_path(:id => order_item), method: :post %>
      </li>
      <li>
        <%= link_to "Remove item", order_item_path(order_item), method: :delete %>
      </li>
    </ul>
    #
  <% end %>
</ul>
<h3>Sub total: <%= number_to_currency(@order.sub_total) %></h3>
# See Order section below for explanation of new_order_path
<%= link_to "Proceed to checkout", new_order_path %><br>
#
<%= link_to "Empty order", order_path(@current_order),method: :delete, data: {confirm: "Are you sure?"}